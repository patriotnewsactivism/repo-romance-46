# Claude Code instructions

The canonical repository operating contract is [`AGENTS.md`](AGENTS.md).

Read `AGENTS.md`, `README.md`, and `docs/PROJECT_STATE.md` before changing code. Read the architecture/operations/security docs when the task touches those areas.

Do not create a separate Claude-specific policy here. If a rule needs to change, update `AGENTS.md` so all coding agents stay consistent.